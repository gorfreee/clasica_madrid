---
type Props = {
  title: string;
  body: string;
};

const { title, body } = Astro.props;
---

<div class="empty-state" role="status">
  <span class="empty-state__shape" aria-hidden="true"></span>
  <h2>{title}</h2>
  <p>{body}</p>
</div>
