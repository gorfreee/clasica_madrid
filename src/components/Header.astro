---
const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const links = [
  { href: '/', label: 'Agenda', current: path === '/' },
  { href: '/lugares', label: 'Lugares', current: path === '/lugares' || path.startsWith('/lugares/') },
];
---

<header class="site-header">
  <div class="site-header__inner">
    <a class="site-mark" href="/" aria-label="Clásica Madrid, ir a la agenda">
      <span>Clásica</span>
      <span>Madrid</span>
    </a>
    <nav aria-label="Principal">
      <ul class="nav-list">
        {
          links.map((link) => (
            <li>
              <a href={link.href} aria-current={link.current ? 'page' : undefined}>
                {link.label}
                {link.current && <span class="nav-list__mark" aria-hidden="true"></span>}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
