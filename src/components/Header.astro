---
const path = Astro.url.pathname.replace(/\/$/, '') || '/';
const links = [
  { href: '/', label: 'Agenda', current: path === '/' },
  { href: '/lugares', label: 'Lugares', current: path === '/lugares' || path.startsWith('/lugares/') },
];
---

<div class="score-rule" aria-hidden="true">
  <span></span>
  <span></span>
  <span></span>
</div>
<header class="site-header">
  <div class="site-header__inner">
    <a class="site-mark" href="/">
      <span class="site-mark__work">Clásica</span>
      <span class="site-mark__place">Madrid</span>
    </a>
    <nav aria-label="Principal">
      <ul class="nav-list">
        {
          links.map((link) => (
            <li>
              <a href={link.href} aria-current={link.current ? 'page' : undefined}>
                {link.label}
              </a>
            </li>
          ))
        }
      </ul>
    </nav>
  </div>
</header>
